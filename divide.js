function divideMachine(input1, input2) {
    const divide = input1 / input2;
    console.log(divide);

    return divide;
}

divideMachine(10, 5);
divideMachine(45, 90);

const result = divideMachine(200, 10);
console.log(result);