function multiplyMachine(input1, input2) {
    let multiply = input1 * input2;
    console.log(multiply);

    return multiply;
}
multiplyMachine(6, 12);
multiplyMachine(10, 20);

const result = multiplyMachine(4, 90);
console.log(result); 