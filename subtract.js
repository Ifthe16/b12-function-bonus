function subtractMachine(num1, num2) {
    var sub = num1 - num2;
    console.log(sub);

    return sub;
}

subtractMachine(300, 500);
subtractMachine(-200, 700);

const result = subtractMachine(40, 80);
console.log(result);